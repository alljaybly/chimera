{
  "name": "necronomicon-mcp",
  "version": "0.1.0",
  "description": "MCP server for legacy protocol adapters (Gopher, Telnet)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "telnet-client": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.7",
    "typescript": "^5.3.3",
    "tsx": "^4.7.0",
    "vitest": "^1.0.4"
  }
}
